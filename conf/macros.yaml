---
- macro:
    name: conf_volume
    volumes:
      - src: '{CONF_PATH}'
        dst : '{ASSURE}'
      - inner_volume:
          CONF_PATH: '{CONF_PATH}'
          ASSURE: '{ASSURE}'


- macro:
    name: inner_volume
    volumes:
      - src: '{CONF_PATH}'
        dst : '{ASSURE}'
